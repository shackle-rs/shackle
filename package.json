{
	"name": "shackle",
	"private": true,
	"scripts": {
		"parser:build": "npm run build -w tree-sitter-minizinc",
		"parser:test": "npm run test -w tree-sitter-minizinc",
		"vscode:build": "npm run compile -w vscode-shackle",
		"vscode:watch": "npm run watch -w vscode-shackle",
		"vscode:lint": "npm run lint -w vscode-shackle",
		"format": "prettier -w .",
		"format:check": "prettier -c ."
	},
	"workspaces": [
		"parsers/tree-sitter-minizinc",
		"editors/code"
	],
	"devDependencies": {
		"prettier": "^3.0.0"
	}
}
